//in a real enviroment we would get the url from a .env file
//but we are using vite.config.js
//TODO: move this to a .env file and logic for working with docker
export const ROUTES = {
  CORN: "/api/corn",
}
